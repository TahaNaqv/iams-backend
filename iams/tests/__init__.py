"""IAMS test package.

Tests live in this package so pytest can discover them automatically.
The legacy ``iams/tests.py`` module remains for backward compatibility
and is also collected by pytest via the ``python_files`` config.
"""
