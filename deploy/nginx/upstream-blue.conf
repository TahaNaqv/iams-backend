# Blue upstream: routes traffic to backend-blue / frontend-blue.
# Symlink ``upstream.conf -> upstream-blue.conf`` to make blue live.
upstream iams_backend  { server backend-blue:8000;  keepalive 16; }
upstream iams_frontend { server frontend-blue:8080; keepalive 16; }
