# Green upstream: routes traffic to backend-green / frontend-green.
# Symlink ``upstream.conf -> upstream-green.conf`` to make green live.
upstream iams_backend  { server backend-green:8000;  keepalive 16; }
upstream iams_frontend { server frontend-green:8080; keepalive 16; }
